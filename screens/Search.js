import React from "react";
import { StyleSheet, Text, View, SafeAreaView } from "react-native";

const Search = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Text>This is List of Films</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});

export default Search;
